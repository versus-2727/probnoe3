name: Sync Wiki to Pages
on:
  gollum:
jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lonnen/sync-wiki-to-branch-action@v1
        with:
          branch: docs
          github_token: ${{ secrets.GITHUB_TOKEN }}
